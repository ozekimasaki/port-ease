## ポート番 {version}

Windows 用です。追加のランタイムは不要です。

- `PortBan-Setup-{version}.exe` はインストーラーです。スタートメニューに追加し、設定からアンインストールできます。管理者権限は不要です。
- `PortBan-{version}.exe` はインストールしない単体版です。

インストール先は `%LOCALAPPDATA%\Programs\PortBan` です。
